---
trigger: always_on
---

1. Use `uv` to manage Python dependencies.
2. Use `uv run /path/to/script.py` instead of `python /path/to/script.py` to run Python scripts.
3. Run `gpkg setup` if errors happen when installing pip packages.